% Transcription — fonds Alexandre Grothendieck, shelfmark 18, batch 4 (page 61).
% Established from the facsimile archives/batches/18/batch-04.pdf (a mirror of
% https://grothendieck.umontpellier.fr/18.pdf), per the skill
% transcribe-grothendieck. The batch file has no cover sheet, so its PDF page 1
% is archivists' page 61; this was checked against the number pencilled at the
% bottom left of the leaf, which reads 61.
%
% Pass: Opus 5 (claude-opus-5), 2026-09-16 — first pass, unchecked against the pages by a human.
%
% The folder's last leaf, and a single page. It carries his hand throughout and
% nothing else: one short line of prose, then a chain of displayed identities,
% a marginal N.B., and a boxed conclusion. Batches 1, 2 and 3 were transcribed
% at the same time by other passes, so no earlier .tex fixed the notation.
%
% The run comes in from page 60, which was glanced at for that purpose only:
% page 60 constructs sigma |-> sigma_* and the homomorphism phi_v from
% Gal(k/k_0) to G(A_C), and closes on the image of Gal(k/k_0) being dense in
% G(A_C). Page 61 asks how that homomorphism depends on the chosen w. Starting
% from M_w^Z = g M_v^Z it computes M_{sigma w}^Z, reads off the twisted
% sigma'_* = sigma-dot g sigma-dot^{-1} sigma_* g^{-1}, notes that at a finite
% place sigma'_l = sigma_l, and at infinity gets sigma'_inf as g_inf conjugated
% by sigma-dot_inf — whence the boxed phi_w(sigma) = g_inf phi_v(sigma)
% g_inf^{-1}: phi_w and phi_v are conjugate under g_inf whenever w and v are
% M-equivalent. Page 59 is not his: it is a printed plate of the G_2 root
% system, and is not transcribed.
%
% Notation fixed for the page: M_w^Z the lattice attached to w, and M_v^Z to v;
% the dot over sigma is his and marks an operator distinct from sigma_*, which
% he writes out in full on the same lines — it is kept as \dot{\sigma}; the
% prime marks the twisted operator sigma'_*; subscripts l and infinity are the
% places; g_infinity^{\dot\sigma_\infty} is his own shorthand for the conjugate
% \dot\sigma_\infty g_\infty \dot\sigma_\infty^{-1}, written as a superscript.
% Z is set \mathbb{Z} throughout, as he writes a blackboard Z.
%
% Left \ill{}: two words of the sentence following « d'où » (the second ends in
% -ment); one word of the marginal N.B.; the character he re-inked between G
% and the parenthesis on the last line; the struck groups inside the two long
% computations; and the tail of the subscript of M at the end of the second
% line of the long computation, which runs off the right edge of the leaf after
% its sigma. The chain of that computation is one line on the leaf; it is
% broken over three aligned lines here so that it fits the measure.
\documentclass[11pt,a4paper]{article}
\input{../preamble/grothendieck}

\folder{18}
\batch{4}
\pages{61}{61}
\dating{[à partir de 1969-vers 1972]}
\watermark{Édition de démonstration}
\foldertitle{Motifs et théorie de Hodge : notes manuscrites (s.d.)}

\begin{document}

\page{61}

\note{Le feuillet reprend en cours de calcul : la page 60 s'achève sur
l'homomorphisme $\varphi_{v}$ de $\mathrm{Gal}(k/k_{0})$ dans $G(A_{\mathbb{C}})$
et sur la densité de son image ; la présente page en tire la variance en $w$.
Le point qu'il met sur $\sigma$ est de lui : il distingue $\dot{\sigma}$ de
$\sigma_{*}$, qu'il écrit en toutes lettres sur les mêmes lignes.}

\marginal{N.B. c'est d'une petite \ill{} vérification par $w$}

Cette vérification peut \uncertain{d'ailleurs} si $w' = \sigma w$
\note{La phrase reste en suspens : elle n'a pas de verbe et passe directement
au calcul.}

\[
  M_{w}^{\mathbb{Z}} = g\,M_{v}^{\mathbb{Z}}
\]

\begin{align*}
  M_{\sigma w}^{\mathbb{Z}} &= \dot{\sigma}(M_{w}^{\mathbb{Z}})
    = \dot{\sigma}\,g\,M_{v}^{\mathbb{Z}}
    = \dot{\sigma}\,g\,\dot{\sigma}^{-1}\,
      (\dot{\sigma} \cdot M_{v}^{\mathbb{Z}}) \\
  &= (\dot{\sigma}\,g\,\dot{\sigma}^{-1})\,\struck{\ill{}}\;\sigma_{*}\,
    M_{\sigma\ill{}}^{\mathbb{Z}} \\
  &= \dot{\sigma}\,g\,\dot{\sigma}^{-1}\,\struck{\ill{}}\;\sigma_{*}\,g^{-1}
    \;\; g\,M_{v}^{\mathbb{Z}}
\end{align*}

\note{Le dernier membre de la deuxième ligne du calcul est écrit au ras du bord
droit du feuillet : l'indice de $M$ commence par $\sigma$ et se termine hors
du champ. Le groupe biffé qui précède $\sigma_{*}$ ne se lit ni ici ni à la
ligne suivante.}

i.e.

\[
  M_{\sigma w}^{\mathbb{Z}} = \struck{\ill{}}\;\sigma'_{*}\,M_{w}^{\mathbb{Z}}
\]

\[
  \sigma'_{*} = \dot{\sigma}\,g\,\dot{\sigma}^{-1}\,\sigma_{*}\,g^{-1}
\]

\note{Au-dessus de cette dernière égalité, une première rédaction biffée d'un
long trait, dont on lit encore $\varphi_{v}(\sigma)\,g^{-1}$ à son extrémité
droite : c'est déjà la formule encadrée plus bas.}

On a donc
\[
  \sigma'_{\ell} = \sigma_{\ell}\,g_{\ell}\,\sigma_{\ell}^{-1}\,
    \sigma_{\ell}\,g_{\ell}^{-1} = \sigma_{\ell}
\]
\note{« pour $\ell$ fini » est ajouté au-dessus de cette formule, relié par un
trait.}

d'où \ill{}

\begin{align*}
  \sigma'_{\infty} &= \dot{\sigma}_{\infty}\,g_{\infty}\,
     \dot{\sigma}_{\infty}^{-1}\,\sigma_{\infty}\,g_{\infty}^{-1} \\
  &= g_{\infty}^{\dot{\sigma}_{\infty}}\,\sigma_{\infty}\,g_{\infty}^{-1}
\end{align*}

i.e.\ \uncertain{on a} :

\[
  \struck{\varphi_{w}(\sigma) = g_{\infty}^{\dot{\sigma}_{\infty}}\,
    \varphi_{v}(\sigma)\,g_{\infty}}
\]

\[
  \boxed{\;\varphi_{w}(\sigma) = g_{\infty}\,\varphi_{v}(\sigma)\,
    g_{\infty}^{-1}\;}
\]

pour $M_{w}^{\mathbb{Z}} = g\,M_{v}^{\mathbb{Z}}$,
$g \in G\ill{}(M_{k}^{(\infty)})$, i.e.\ si $w$ et $v$ sont
\emph{M-équivalents}.
\note{Entre $G$ et la parenthèse, un caractère repassé plusieurs fois à
l'encre ne se lit pas ; la page 60 écrit $G(A_{\mathbb{C}})$ au même endroit
de la notation. Au-dessus du $g_{\infty}$ encadré subsiste un petit signe,
reste de la rédaction biffée. « M-équivalents » est souligné de sa main.}

\end{document}
